{
  "name": "docker-image-artifact",
  "version": "1.2.0",
  "description": "Package for github action image artifact",
  "main": "./src/image_artifact.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "github-actions",
    "docker",
    "image",
    "artifact"
  ],
  "author": "Ishwor Khadka",
  "license": "MIT",
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@actions/artifact": "^1.1",
    "child_process": "^1.0",
    "zlib": "^1.0"
  }
}
